/// @description Insert description here
// You can write your code in this editor

if info == noone {
	info = instance_create_depth(x, y + sprite_height/2 + 10, depth - 1, o_build_menu_info);
	info.text = GetResourceName(metal);
}